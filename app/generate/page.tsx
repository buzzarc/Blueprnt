import { GeneratePage } from '@/components/pages/generate-page'

export const metadata = {
  title: 'Generate a Roadmap — Blueprnt',
  description: 'Paste your idea dump and let AI turn it into a staged, personalized roadmap with milestones, tools, tasks, and checkpoints.',
}

export default function Page() {
  return <GeneratePage />
}
